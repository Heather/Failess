[<AutoOpen>]
module Failess.CSSTypes

open System
open System.ComponentModel

type Clear =
    | both = 0

type TextDecoration =
    | none = 0

type TextAlign =
    | center = 0
    | left = 0
    | right = 0

type FontWeight =
    | bold = 0

type WhiteSpace =
    | nowrap = 0

type Border =
    | solid = 0

type Position =
    | relative = 0

type LineHeight =
    | normal = 0

type ListStyle =
    | none = 0

type Display =
    | block = 0